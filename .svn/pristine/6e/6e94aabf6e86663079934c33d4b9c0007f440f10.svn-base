package kr.or.ddit.gmail.dao;

import static org.junit.jupiter.api.Assertions.fail;

import java.util.HashMap;
import java.util.Map;

import javax.inject.Inject;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

import kr.or.ddit.annotation.RootContextWebConfig;
import kr.or.ddit.commons.paging.PaginationInfo;
import lombok.extern.slf4j.Slf4j;

@Slf4j
@RootContextWebConfig
class MailSentMapperTest {
	@Inject
	private MailSentMapper mapper;

	@Test
	void testCase1() {
		mapper.selectSentMailDetail("18b483aecdce154e");
	}


	@Disabled
	@Test
	void testCase2() {
		PaginationInfo paging = new PaginationInfo();
		paging.setCurrentPage(1);
		Map<String, Object> variousCondition = new HashMap<>();
		variousCondition.put("empMail", "kimny950111@gmail.com");
		variousCondition.put("type", "title");
		variousCondition.put("value", "Re:");
		
		
		paging.setVariousCondition(variousCondition);
		
		
		mapper.selectSentMailList(paging);
	}

}
